package java_essentials.Java17.FirstLook.sealedInterface;

//the child interface of a sealed interface must be either sealed or non-sealed
public non-sealed interface SealedSubInterfaceExample   extends SealedInterfaceExample {
}
